#include <stdio.h>

int main()
{
    /* Ponteiros são variáveis que armazenam o endereço de memória de outras variáveis, sendo muito
       úteis quando uma variável deve ser acessada em diferentes lugares de um programa. Graças aos
       ponteiros também é possível realizar a manipulação de memória, o que torna a solução de problemas
       de baixo nível muito mais precisos. */
}
